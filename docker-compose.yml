version: '3.9'
services:
  gatekeeper:
    image: golang:1.22
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: ["go", "run", "./cmd/gatekeeper"]
    ports:
      - "8080:8080"
